{
  "identifier": "mobile",
  "description": "PDF小僧のモバイル(Android/iOS)向け権限。default.json は platforms が windows/linux/macOS 限定のため、モバイルではここで別途許可する必要がある。",
  "windows": ["main"],
  "platforms": ["android", "iOS"],
  "permissions": [
    "core:default",
    "core:webview:allow-set-webview-zoom",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "dialog:allow-message",
    "fs:default",
    "fs:allow-mkdir",
    "fs:read-files",
    "fs:read-dirs",
    "fs:write-files"
  ]
}

